<resources>
    <!-- Base application theme. -->
    <style name="Theme.SubmissionAndroidSederhana" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow_accent</item>
        <item name="colorPrimaryVariant">@color/yellow_accent_light</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow_card_light</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="TextAppTitle">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">24sp</item>
    </style>
    <style name="ToolbarTheme">
        <item name="colorControlNormal">?attr/colorPrimary</item>
        <item name="color">@color/white</item>
        <item name="tint">?attr/colorPrimary</item>
    </style>
</resources>